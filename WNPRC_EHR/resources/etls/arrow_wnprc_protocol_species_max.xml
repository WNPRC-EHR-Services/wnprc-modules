<etl xmlns="http://labkey.org/etl/xml">
  <name>Arrow ETL</name>
  <description>Add description</description>
  <transforms>
    <transform id="step1" type="org.labkey.di.pipeline.TransformTask">
      <description>Copy to target</description>
      <!--new query to union with extra rows-->
      <source queryName="wnprc_species_max" schemaName="arrow"/>
      <destination queryName="protocol_species_max" schemaName="wnprc"/>
    </transform>
  </transforms>
  <!-- possibly a merge ETL that only copies the max species and protocol column -->
  <!-- or copy to a temp table and set up an ETL that joins everything -->
  <!--<incrementalFilter className="ModifiedSinceFilterStrategy" timestampColumnName="modified"/>-->
  <schedule>
    <poll interval="1h"/>
  </schedule>
</etl>

